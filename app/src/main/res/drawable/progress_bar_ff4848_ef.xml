<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- background -->
    <item
        android:id="@android:id/background"
        android:drawable="@color/white_ef"></item>
    <!-- progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="0"
                    android:endColor="@color/red_ff4848"
                    android:startColor="@color/red_ff4848" />
            </shape>
        </clip>
    </item>

</layer-list>