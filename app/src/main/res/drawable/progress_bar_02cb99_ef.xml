<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- background -->
    <item
        android:id="@android:id/background"
        android:drawable="@color/white_ef"></item>
    <!-- progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="0"
                    android:endColor="@color/green_02cd99"
                    android:startColor="@color/green_02cd99" />
            </shape>
        </clip>
    </item>

</layer-list>